# docker-compose.yml fayli
name: uytop-bot
version: "3.9"

services:
  bot:
    build: .
    container_name: uytop_bot
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env
    volumes:
      - .:/app
